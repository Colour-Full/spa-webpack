import path from 'path'
import {
  rules,
  output,
  extentions,
  alias,
  HtmlWebpackPlugin,
  webpackCleanPlugin,
  FlowBabelWebpackPlugin,
  webpack
} from 'spa-webpack'

// The public path for dev build should always default to '/'
output.publicPath = '/'

module.exports = {
  mode: 'development',
  entry: ['babel-polyfill', path.resolve(__dirname, 'node_modules/spa-webpack/node_modules/react-hot-loader/patch'), path.resolve('./', 'src/index.js')],
  module: {
    // This is added to avoid warnings generated by some of the dev tools
    // The prod Webpack config should compile fine without it
    exprContextCritical: false,
    rules: rules
  },
  devtool: 'inline-source-map',
  devServer: {
    contentBase: path.resolve(__dirname, 'dist'),
    hot: true
  },
  plugins: [
    webpackCleanPlugin,
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'node_modules/spa-webpack/index.html')
    }),
    new FlowBabelWebpackPlugin(),
    new webpack.LoaderOptionsPlugin({ options: {} })
  ],
  output: output,
  resolve: {
    alias: alias,
    extensions: extentions
  },
  resolveLoader: {
    modules: [path.resolve(__dirname, 'node_modules/spa-webpack/node_modules/'), 'node_modules']
  },
  node: {
    dns: 'mock',
    net: 'mock'
  },
  externals: {
    winston: 'winston'
  }
}
